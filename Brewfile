# vim: set ft=conf

# update homebrew itself
update

# add repositories
tap homebrew/versions || true

# packages
install ctags
install jq
install tree
install wget
install tmux
install rlwrap
install readline
install coreutils
install the_silver_searcher

# remove outdated versions
cleanup
